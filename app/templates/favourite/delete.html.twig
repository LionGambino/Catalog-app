{% include 'default/_form.html.twig' with {
    page_title: 'title.element_delete_favourite'|trans({'%id%': element.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'element_index',
} %}
